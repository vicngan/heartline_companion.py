Drop your branded `icon.png`, `adaptive-icon.png`, and `splash.png` in this folder before building.

For quick testing you can swap these paths in `app.json` to Expo defaults or temporary placeholders.
